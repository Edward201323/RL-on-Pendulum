# Build output (generated by CMake)
build/

# Debug symbol bundles
*.dSYM/

# Python caches
__pycache__/
*.pyc

# Trainer runtime artifacts / saved training data. All regenerated by reinforce.py
# and persisted locally between runs (wiped with Command+Shift+R) -- not source.
trainer/policy.pt
trainer/policy.txt
trainer/actors.txt
trainer/train_agents.txt
trainer/train_history.txt
trainer/train_status.txt
trainer/train_speed.txt

# macOS
.DS_Store
